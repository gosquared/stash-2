{
  "name": "@gosquared/stash-2",
  "version": "0.1.3",
  "description": "",
  "main": "dist/src/main.js",
  "scripts": {
    "build": "rm -rf dist && tsc || exit 0",
    "watch": "rm -rf dist && tsc -w",
    "pretest": "docker-compose up -d redis",
    "cleanup": "docker-compose stop && docker-compose rm -f",
    "posttest": "npm run cleanup",
    "test": "mocha --exit dist/test",
    "prepublish": "npm run build",
    "monitor": "docker exec -it stash2-redis redis-cli monitor"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@gosquared/quick-lru-cjs": "^6.0.1",
    "@types/chai": "^4.2.21",
    "hashlru": "^2.3.0",
    "ioredis": "^4.27.8",
    "lru-cache": "^6.0.0",
    "redlock": "^4.2.0"
  },
  "devDependencies": {
    "@types/faker": "^5.5.8",
    "@types/ioredis": "^4.26.7",
    "@types/lru-cache": "^5.1.1",
    "@types/mocha": "^9.0.0",
    "@types/node": "^16.7.1",
    "@types/redlock": "^4.0.2",
    "@types/sinon": "^10.0.2",
    "chai": "^4.3.4",
    "faker": "^5.5.3",
    "mocha": "^9.1.0",
    "sinon": "^11.1.2",
    "typescript": "^4.3.5"
  }
}
